name = "one-life-strava"
main = "workers/strava-token.js"
compatibility_date = "2024-01-01"

# After deploy, set secrets (one-time):
#   npx wrangler secret put STRAVA_CLIENT_ID
#   npx wrangler secret put STRAVA_CLIENT_SECRET
#
# Worker URL → GitHub secret VITE_STRAVA_TOKEN_PROXY
